name: collections-docker-build
on:
  push:
    branches:
      - main
    paths:
      - docker/packages/collections.Dockerfile
      - docker/packages/.cargo/**
      - server/packages/collections/**
      - server/common/cors/**
      - server/common/proto/**
      - .github/workflows/collections.yaml

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: git clone
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: suxiaoshao/collections:latest
          file: ./docker/packages/collections.Dockerfile
          context: .
