import CollectionConfig from 'collections';
import BookmarkConfig from 'bookmarks';
import { MicroConfig } from 'types';
export const microConfigs: MicroConfig[] = [new CollectionConfig(), new BookmarkConfig()];
