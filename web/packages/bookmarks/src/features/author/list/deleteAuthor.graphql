mutation deleteAuthor($id: Int!) {
  deleteAuthor(id: $id) {
    __typename
    ... on ResourceDeleted {
      resource {
        kind
        id
      }
    }
    ... on ValidationFailure {
      issues {
        path
        code
        min
        max
      }
    }
  }
}
