query getAuthors($pagination: Pagination!) {
  queryAuthors(pagination: $pagination) {
    data {
      id
      site
      name
      createTime
      updateTime
      avatar
      description
    }
    total
  }
}
