mutation createTag($name: String!) {
  createTag(name: $name) {
    name
    id
  }
}
query getTags {
  queryTags {
    name
    id
    createTime
    updateTime
  }
}

mutation deleteTag($id: Int!) {
  deleteTag(id: $id) {
    id
  }
}
