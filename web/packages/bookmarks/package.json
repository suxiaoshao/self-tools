{
  "name": "bookmarks",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "theme": "workspace:theme",
    "notify": "workspace:notify",
    "auth": "workspace:auth"
  }
}
