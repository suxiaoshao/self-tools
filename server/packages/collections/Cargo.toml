[package]
name = "collections"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# runtime
tokio = { version = "1.26.0", features = ["macros", "rt-multi-thread"] }

# graphql
async-graphql = { version = "5.0.6", features = ["time"] }
axum = { version = "0.6.11", features = ["headers"] }
async-graphql-axum = "5.0.6"
serde_json = "1.0.94"

# database
diesel = { version = "2.0.3", features = ["postgres", "time", "r2d2"] }
time = "0.3.20"
once_cell = "1.17.1"

# error
anyhow = "1.0.70"
thiserror = "1.0.40"

# cors
middleware = { path = "../../common/middleware",features=["cors","trace","graphql-trace"] }

# grpc
proto = { path = "../../common/proto", features = ["auth_client"] }
tonic = "0.8.3"

# log
tracing = "0.1.37"
tracing-subscriber = "0.3.16"

[dev-dependencies]
# env
dotenv = "0.15.0"
